#include <stdio.h>

int main(void) {
    const char *hw = "Hello, World!";

    printf("%s\n%s\n%s\n", hw, hw, hw);
    // Hello, World!
    // Hello, World!
    // Hello, World!
    
    return 0;
}
